import MdxWrapper from "../components/mdx-wrapper"
import ProjectsList from "../components/projects-list"
import Seo from "../components/seo"
import { Link } from "gatsby"
export const Head = () => <Seo title="Projects"/>
export default ({children}) => <MdxWrapper title="Projects">{children}</MdxWrapper>

<p style={{textAlign: "center"}}>Here are some of our recent projects. Click on a project to read more.</p>

<div className="center-content">
  <div style={{
    width: "900px", 
    maxWidth: "100vw",
    margin: "50px 0"
  }}>
    <ProjectsList/>
  </div>
</div>

<h3 style={{textAlign: "center"}}>Get in touch with us to discuss your next project</h3>

<div className="btn-multiple-wrapper">
  <Link to="/contact" className="btn btn-yellow">Contact Us</Link>
</div>